{
  "name": "thou-art-that-microsite",
  "version": "0.1.0",
  "private": true,
  "description": "Static microsite for Thou Art That, a study piece on working with possibly-emergent AI. Deployed to tat.marbl.codes.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch & npx --yes serve dist -p 4321",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^13.0.0",
    "wrangler": "^4.87.0"
  }
}
