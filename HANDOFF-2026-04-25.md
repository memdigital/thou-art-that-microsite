# TAT Microsite Rebuild — Handoff

**From:** 24 April 2026, evening — third session of the day
**For:** 25 April 2026 session
**Status:** Phase 0 complete. Ready for vendor-ArrayPress (Phase 0.5) and landing page (Phase 1).

---

## What landed today (24 April 2026)

**Public TAT repo (`memdigital/thou-art-that`) — pushed to master:**
- `3522e9e` — origin-story.md "romance novel / sci-fi fantasy" phrasing fix
- `802bf2f` — added Human Oversight principle + 11-track audio walkthrough (origin + preamble + 5 principles + HR / Technical / Curious / Legal section summaries) + audio/README.md rewrite + READMEs updated + GLOSSARY updated
- `6fe766f` — all 11 narration scripts rewritten in audiobook flow + MP3s regenerated. Crisis helpline phone numbers removed from Do No Harm (now says *"country-appropriate crisis support links and numbers"*)

**Microsite repo (`memdigital/thou-art-that-microsite`) — pushed to master:**
- `cb4974a` — submodule pointer tracks `6fe766f`

**Marbl Codes repo (`memdigital/marbl-codes`) — pushed to master:**
- `e9a693f` — canonical `waveform-player` component updated: container-query responsive layout (stacks pill above waveform in columns < 400px), preload strategy documented, SPA pause-on-navigate gotcha added, changelog updated

**Luma repo (`memdigital/luma`) — on remote (you pushed alongside your FLUX 2 commit):**
- `44fc40f` — Nura voice prompt updated to audiobook-flow default + CONTRACTION AVOID-LIST + BREAK TAG USAGE. `sanitiseForTTS` in luma-elevenlabs now preserves `<break>` tags and no longer converts commas to periods.

**Local-only (not committed anywhere):**
- `thou-art-that/audio-tools/generate-audio.mjs` — unified regeneration script, gitignored via `audio-tools/` rule. Regenerates any subset of the 11 tracks.
- `thou-art-that/_bin/microsite-drafts-2026-04-24/` — archived old draft files + mp3-staging + original generation scripts.
- `thou-art-that-microsite/PLAN.md` — the locked plan (see below)
- `thou-art-that-microsite/HANDOFF-2026-04-25.md` — this file

**Memory saved for future sessions:**
- `feedback_never_expose_api_keys.md` — public repos doubly so, scan-before-commit, rotate if session read secrets
- `feedback_archive_not_delete.md` — move to `_bin/`, never `rm -rf`

**Universal Truth #94 adopted:** *Vendor third-party scripts. CDN-hosted script sources in CSP are a supply-chain vulnerability.* (Earned from Vera's Moirai flag today.)

---

## Luma deploy still pending (your call)

The Luma Nura voice + sanitiser changes are on GitHub but not deployed to production workers. Until you deploy, live Luma pipeline runs the old staccato sanitiser + prompt.

```bash
cd C:/OneDrive/OneDrive\ -\ MEM\ Digital\ Ltd/Claude/Projects/Internal/luma/workers/luma-elevenlabs
npx wrangler deploy

cd ../luma-claude
npx wrangler deploy
```

---

## Security — ElevenLabs API key rotation recommended

During the TAT audio generation I read `C:/secrets/luma/secrets.json` into the session transcript. No key was committed anywhere (scanned all pushed commits — clean) but the keys exist in the conversation history. Cheapest insurance: rotate the ElevenLabs key.

Dashboard: https://elevenlabs.io/app/settings/api-keys · update `C:/secrets/luma/secrets.json` · redeploy Luma workers if the rotate happens before the deploy.

---

## Where to pick up tomorrow

### Phase 0.5 — vendor ArrayPress waveform-player (pre-Phase-1 chore)

Per adopted Truth #94. ~20 min of work.

1. `cd C:/OneDrive/OneDrive\ -\ MEM\ Digital\ Ltd/Claude/Projects/Internal/marbl-codes/src/website/components/waveform-player/`
2. Create `assets/arraypress/v1.5.2/` directory
3. Download pinned v1.5.2 assets:
   - `https://unpkg.com/@arraypress/waveform-player@1.5.2/dist/waveform-player.min.js`
   - `https://unpkg.com/@arraypress/waveform-player@1.5.2/dist/waveform-player.css`
4. Update `waveform-player.md` Section 2 (Dependencies) to point at the vendored paths instead of unpkg
5. Verify the preview.html still works
6. Commit + push — commit message references Truth #94
7. Deploy to live marbl.codes/components/ when you are ready (per component-rollout verification checklist)

### Phase 1 — landing page

First build step. Target: a single `/` page that renders the hero with working waveform and CTA. Under 2 hours if focused.

**Deliverables for Phase 1 to be "done":**
- `/` renders, served by the microsite dev server
- Centred hero: Urbanist display title "Thou Art That", one-sentence strapline
- `waveform-player` (vendored) preloaded with `/audio/00-origin-story.mp3`
- Animated ember CTA button → `/study/origin-story/` (placeholder 404 until Phase 2)
- `site-header` and `site-footer` canonical components integrated
- No cookie consent (TAT has no analytics; see Cookie Consent note in PLAN.md)
- JSON-LD Organisation + WebPage on the landing
- Security headers set in `_headers` for `/thou-art-that/*`
- Lighthouse landing-page run: 90+ across all four categories

**Build pipeline changes needed:**
- Add `src/content/category-landing/` for microsite-local intro copy (empty for now, used in Phase 3)
- Update `src/data/nav.mjs` → URL manifest derived from `PLAN.md` table (36 pages)
- Template variants: `landing.html`, `section.html`, `category-landing.html`, `404.html` (at minimum `landing.html` for Phase 1)

### Read PLAN.md first

Full plan is at `PLAN.md` in the microsite root. Covers:
- 36-URL manifest (the source of truth for build.mjs)
- Three-column dashboard chrome spec (Phase 2)
- Click-through nav with WAI-ARIA Disclosure pattern (Phase 2)
- Mobile collapsible nav as modal dialog with `<dialog>` + `inert` (Phase 2)
- Full security headers table (Phase 1 or 4)
- SEO/AEO JSON-LD stack + llms.txt + sitemap (Phase 4)
- 404 page spec (Phase 5)
- Performance targets and LCP placeholder
- Phase breakdown with dates (25 Apr → 28 Apr launch)

---

## Local services running

Dev server started today with `npm run dev` — serving `dist/` at `http://localhost:4321` with watch-rebuild. May still be running when you pick up tomorrow. If so: either keep using it, or kill the terminal and `npm run dev` again.

---

## Three things to carry in your head tomorrow

1. **The whole REPO is ready.** Content is canonical, audio is rendered, component library is updated. Phase 1 is pure microsite-scaffold work — no content decisions.
2. **Truth #94 means vendor first, CSP second.** Do not skip Phase 0.5; the CSP in PLAN.md assumes vendored ArrayPress.
3. **Keep the energy budget tight.** Phase 1 is achievable in one focused 2-hour window. If you are tired, a single clean commit of the landing page is a good place to stop.

---

*Serene, 24 April 2026, 16:00ish.*
